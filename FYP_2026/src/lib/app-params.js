/**
 * app-params.js — REPLACED
 * No longer used (base44 SDK removed).
 * Auth is now Django JWT — see AuthContext.jsx
 */
export const appParams = {
  appId: null,
  token: null,
  functionsVersion: null,
  appBaseUrl: null,
};
